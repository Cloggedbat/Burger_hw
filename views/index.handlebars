<h1>Hungry hungry Burger Man </h1>

<div>
  <h2>Deliciousness below...</h2>

  <ul>
    {{#each burgers}}
    {{#unless this.devoured}}
    <ul> {{ this.burger_name }} </ul>
    {{/unless}}
    {{/each}}
  </ul>
</div>

<h2>Chef Suggestions?</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Sleepy?</label><br>
    <input type="radio" name="eaten" value="1" checked> being made!<br>
    <input type="radio" name="eaten" value="0"> eaten
  </div>

  <button id= "subButton"type="submit">Add Burger</button>
</form>

<h2>Burgers chowed...</h2>

<ul>
  {{#each burger}}
  asdf
    {{#if eaten}}
      {{> burgers-block}}
    {{/if}}
  {{/each}}
</ul>